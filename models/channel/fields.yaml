# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: idesigning.feedback::lang.channel.name

    code:
        label: idesigning.feedback::lang.channel.code
        preset:
            field: name
            type: slug

    method:
        label: idesigning.feedback::lang.channel.method
        type: dropdown

    prevent_save_database:
        label: idesigning.feedback::lang.backend.settings.channel.preventSaveDatabase
        type: checkbox

    warning:
        type: hint
        path: @/plugins/idesigning/feedback/controllers/channels/_no_action_warning.htm